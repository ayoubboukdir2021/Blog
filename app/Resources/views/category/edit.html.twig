{% extends 'back.html.twig' %}

{% block content %}
    <h1>Category edit</h1>

    {{ form_start(edit_form) }}
        <div class="form-group">
             {{ form_row(edit_form.libelle , {'required':true,'attr':{'class':'form-control'}} ) }}
        </div>

        <div class="form-group">
             {{ form_row(edit_form.description , {'required':true,'attr':{'class':'form-control'}}) }}
        </div>
       
        <input class="btn btn-primary" type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

{% endblock %}

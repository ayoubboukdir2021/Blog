{% extends 'back.html.twig' %}

{% block content %}

    {{ form_start(form) }}

        <div class="form-group">
            {{ form_row(form.libelle , {'required':true,'attr':{'class':'form-control'}} ) }}
        </div>

        <div class="form-group">
            {{ form_row(form.description , {'required':true,'attr':{'class':'form-control'}}) }}
        </div>

        <input class="btn btn-primary" type="submit" value="Create" />

        <a class="btn btn-info" href="{{ path('admin_category_index') }}">Retour</a>

    {{ form_end(form) }}


{% endblock %}


{% block breadcrumb %}
    <ul class="breadcrumb">
      <li class="breadcrumb-item"><a href="{{ path('admin_index_user') }}">Tableau de bord</a></li>
      <li class="breadcrumb-item active">Nouveau categorie</li>
    </ul>
{% endblock %}
